# Base URL of the Smart Waste backend API (see ../New folder/.env.example PORT)
VITE_API_BASE_URL=http://localhost:4000/api/v1

# How often live views poll the backend, in milliseconds
VITE_POLL_INTERVAL_MS=10000
